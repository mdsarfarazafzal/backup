* Studied Proxy and Reflect APIs
* Revised custom events
* Explored AbortController for cancelling async operations
__proto__ is a pointer that links the instance to the constructor's prototype, every object has __proto__
